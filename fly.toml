# fly.toml — деплой: fly launch && fly secrets set ... && fly deploy

app = "vikalashme-bot"
primary_region = "ams"

[build]
  dockerfile = "Dockerfile"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"

[processes]
  app = "python main.py"
