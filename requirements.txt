aiogram==3.29.1
python-dotenv==1.1.0
certifi==2025.6.15
# Plain pin — no [asyncio] extra. Some host pip parsers mishandle brackets
# in requirements lines; greenlet (needed for asyncio) is listed explicitly.
sqlalchemy==2.0.41
greenlet==3.2.3
aiosqlite==0.21.0
asyncpg==0.30.0
apscheduler==3.10.4
# The whole schedule is computed in Europe/Moscow; slim base images do not
# always ship /usr/share/zoneinfo, and ZoneInfo would then fail at import.
tzdata==2026.3
